---
name: step3_method_at_intake_m
description: Reported use of method at intake
priority: 1
condition: "(step3_method_on_file == '' || step3_method_on_file == 'no_method') && global_method_gender_type == 'Male'"
actions:
  - "isRelevant = true"
---
name: step3_method_at_intake_f
description: Reported use of method at intake
priority: 1
condition: "(step3_method_on_file == '' || step3_method_on_file == 'no_method') && global_method_gender_type == 'Female'"
actions:
  - "isRelevant = true"
---
name: step3_still_on_method
description: Reported use of method at intake
priority: 1
condition: "(step3_method_on_file != '' && step3_method_on_file != 'no_method')"
actions:
  - "isRelevant = true"
---
name: step3_on_another_method_m
description: Reported use of method at intake
priority: 1
condition: "(step3_still_on_method != 'no' && global_method_gender_type == 'Male')"
actions:
  - "isRelevant = true"
---
name: step3_on_another_method_f
description: Reported use of method at intake
priority: 1
condition: "(step3_still_on_method != 'no' && global_method_gender_type == 'Female')"
actions:
  - "isRelevant = true"
---
name: step3_iud_insertion_date
description: Date of IUD insertion if currently using the method.
priority: 1
condition: "(step3_current_method == 'iud' || step3_current_method == 'cu_iud' || step3_current_method == 'lng_iud')"
actions:
  - "isRelevant = true"
---
name: step3_implant_date
description: Date of last implant if currently using the method.
priority: 1
condition: "(step3_current_method == 'implants' || step3_current_method == 'etg_onerod' || step3_current_method == 'lng_tworod')"
actions:
  - "isRelevant = true"
---
name: step3_last_injection_date
description: Date of last injection if currently using the method.
priority: 1
condition: "(step3_current_method == 'injectable' || step3_current_method == 'dmpa_im' || step3_current_method == 'dmpa_sc' || step3_current_method == 'net_en')"
actions:
  - "isRelevant = true"
---
name: step3_sterilization_date
description: Date of sterilization, if not reversed.
priority: 1
condition: "(step3_current_method == 'male_sterilization' || step3_current_method == 'female_sterilization')"
actions:
  - "isRelevant = true"
---
name: step3_reason_no_method
description: If not using a method, the reason for no contraceptive method use.
priority: 1
condition: "(step3_current_method == 'no_method')"
actions:
  - "isRelevant = true"
---
name: step3_reason_no_method_other
description: The other reason for no contraceptive method.
priority: 1
condition: "(step3_reason_no_method == 'other')"
actions:
  - "isRelevant = true"
---
name: step3_never_used_contraception
description: If the client has never previously used any modern contraceptive methods.
priority: 1
condition: "(step3_current_method == 'no_method')"
actions:
  - "isRelevant = true"
---
name: step3_prior_methods_used_m
description: Contraceptive methods the client has a history of using
priority: 1
condition: "(step3_never_used_contraception == 'no' && global_method_gender_type == 'Male')"
actions:
  - "isRelevant = true"
---
name: step3_prior_methods_used_f
description: Contraceptive methods the client has a history of using
priority: 1
condition: "(step3_never_used_contraception == 'no' && global_method_gender_type == 'Female')"
actions:
  - "isRelevant = true"
---
name: step3_sex_active
description: Whether or not the client is sexually active.
priority: 1
condition: "(step3_ever_had_sex == 'yes')"
actions:
  - "isRelevant = true"
---
name: step3_days_unprotect_sex
description: Days since unprotected sex
priority: 1
condition: "(global_gender == 'Female' && step1_reason_for_visit.contains('emergency_contraception'))"
actions:
  - "isRelevant = true"
---
name: step3_gravida
description: The number of pregnancies, current and past, regardless of pregnancy outcome.
priority: 1
condition: "(global_gender == 'Female' && step3_ever_had_sex == 'yes')"
actions:
  - "isRelevant = true"
---
name: step3_parity
description: The number of pregnancies reaching parity. Zero births is defined as nulliparous. 1 or more births, the client is parous.
priority: 1
condition: "(global_gender == 'Female' && step3_gravida > 0)"
actions:
  - "isRelevant = true"
---
name: step3_delivered_last_six_months
description: Whether the client has deliverd a baby within the past 6 months.
priority: 1
condition: "(global_gender == 'Female' && step3_parity > 0)"
actions:
  - "isRelevant = true"
---
name: step3_delivery_date
description: If the client has delivered in the past 6 months, the date in which she delivered
priority: 1
condition: "(step3_delivered_last_six_months == 'yes')"
actions:
  - "isRelevant = true"
---
name: step3_breastfeeding
description: Select if a woman is currently breastfeeding.
priority: 1
condition: "(global_gender == 'Female')"
actions:
  - "isRelevant = true"
---
name: step3_breastfeeding_status
description: Describe the woman's breastfeeding status
priority: 1
condition: "(step3_breastfeeding == 'yes')"
actions:
  - "isRelevant = true"
---
name: step3_recent_miscarriage_abortion
description: Select if the woman had an abortion or a miscarriage within the past 4 weeks
priority: 1
condition: "(global_gender == 'Female')"
actions:
  - "isRelevant = true"
---
name: step3_date_miscarriage_abortion
description: If the woman had a miscarriage or abortion, when the miscarriage or abortion took place.
priority: 1
condition: "(step3_recent_miscarriage_abortion == 'yes')"
actions:
  - "isRelevant = true"
---
name: step3_miscarriage_abortion_preg_stage
description: If the woman recently had a miscarriage or abortion, when in the pregnancy the abortion took place.
priority: 1
condition: "(step3_recent_miscarriage_abortion == 'yes')"
actions:
  - "isRelevant = true"
---
name: step3_start_date_last_menses
description: First day of last normal menses.
priority: 1
condition: "(global_gender == 'Female')"
actions:
  - "isRelevant = true"
---
name: step3_missed_menses
description: Calculated field to determine the number of days since the woman's last normal period started.
priority: 1
condition: "(global_gender == 'Female')"
actions:
  - "isRelevant = true"
---
name: step3_amenorrhea
description: Calculated field to determine the number of days since the woman's last normal period started.
priority: 1
condition: "(global_gender == 'Female')"
actions:
  - "isRelevant = true"
---



name: step5_issues_concerns_f
description: This field will be shown if reason for visit contains issues & concerns and method_gender_type female
priority: 1
condition: "step1_reason_for_visit.contains('issues_concerns') && global_method_gender_type == 'Female'"
actions:
  - "isRelevant = true"
---
name: step5_issues_concerns_m
description: This field will be shown if reason for visit contains issues & concerns and method_gender_type male
priority: 1
condition: "step1_reason_for_visit.contains('issues_concerns') && global_method_gender_type == 'Male'"
actions:
  - "isRelevant = true"
---
name: step5_issues_concerns_other_f
description: This field will be shown if user check other
priority: 1
condition: "step5_issues_concerns_f.contains('other')"
actions:
  - "isRelevant = true"
---
name: step5_issues_concerns_other_m
description: This field will be shown if user check other
priority: 1
condition: "step5_issues_concerns_m.contains('other')"
actions:
  - "isRelevant = true"
---
name: step5_toaster1
description: This field will be shown if user check other
priority: 1
condition: "step5_issues_concerns_f.contains('prolonged_bleeding') ||
            step5_issues_concerns_f.contains('irregular_bleeding') ||
            step5_issues_concerns_f.contains('amenorrhea') ||
            step5_issues_concerns_f.contains('vaginal_bleeding') ||
            step5_issues_concerns_f.contains('cramping_pain') ||
            step5_issues_concerns_f.contains('anemia') ||
            step5_issues_concerns_f.contains('feel_iudstrings') ||
            step5_issues_concerns_f.contains('abdominal_pain') ||
            step5_issues_concerns_f.contains('perforation') ||
            step5_issues_concerns_f.contains('uid_partial_expul') ||
            step5_issues_concerns_f.contains('uid_complete_expul') ||
            step5_issues_concerns_f.contains('missing_strings') ||
            step5_issues_concerns_f.contains('acne') ||
            step5_issues_concerns_f.contains('ordinary_headache') ||
            step5_issues_concerns_f.contains('breast_tenderness') ||
            step5_issues_concerns_f.contains('weight_change') ||
            step5_issues_concerns_f.contains('nausea') ||
            step5_issues_concerns_f.contains('heart_disease') ||
            step5_issues_concerns_f.contains('migraine_headache') ||
            step5_issues_concerns_f.contains('missed_pills') ||
            step5_issues_concerns_f.contains('late_injection') ||
            step5_issues_concerns_f.contains('sex_drive_change') ||
            step5_issues_concerns_f.contains('suspected_preg') ||
            step5_issues_concerns_f.contains('issues_patch') ||
            step5_issues_concerns_f.contains('issues_ring') ||
            step5_issues_concerns_f.contains('change_in_mp') ||
            step5_issues_concerns_f.contains('blood_clots') ||
            step5_issues_concerns_f.contains('lasting_pain') ||
            step5_issues_concerns_f.contains('infected_incision') ||
            step5_issues_concerns_f.contains('abscess') ||
            step5_issues_concerns_f.contains('difficulty_condom_use') ||
            step5_issues_concerns_f.contains('irritation') ||
            step5_issues_concerns_f.contains('incorrect_condom_use') ||
            step5_issues_concerns_f.contains('difficult_partner') ||
            step5_issues_concerns_f.contains('condom_squeaks') ||
            step5_issues_concerns_f.contains('other')
"
actions:
  - "isRelevant = true"
---
name: step5_toaster2
description: This field will be shown if user check other
priority: 1
condition: "step5_issues_concerns_f.contains('thrombosis') ||
            step5_issues_concerns_f.contains('ectopic_pregnancy') ||
            step5_issues_concerns_f.contains('heart_attack') ||
            step5_issues_concerns_f.contains('liver_disorders') ||
            step5_issues_concerns_f.contains('pid') ||
            step5_issues_concerns_f.contains('pulmonary_embolism') ||
            step5_issues_concerns_f.contains('ruptured_ectopic_preg') ||
            step5_issues_concerns_f.contains('reaction_to_latex') ||
            step5_issues_concerns_f.contains('stroke') ||
            step5_issues_concerns_f.contains('shock_syndrome')
"
actions:
  - "isRelevant = true"
---
name: step7_method_exit_f
description: Contraceptive method at exit.
priority: 1
condition: "global_method_gender_type == 'Female'"
actions:
  - "isRelevant = true"
---
name: step7_methods_exit_secondaryf
description: Secondary contraceptive method at exit.
priority: 1
condition: "step7_method_exit_f == 'no_method' && global_method_gender_type == 'Female'"
actions:
  - "isRelevant = true"
---
name: step7_method_exit_m
description: Contraceptive method at exit.
priority: 1
condition: "global_method_gender_type == 'Male'"
actions:
  - "isRelevant = true"
---
name: step7_methods_exit_secondary_m
description: Secondary contraceptive method at exit.
priority: 1
condition: "global_method_gender_type == 'Male' && step7_method_exit_m == 'no_method'"
actions:
  - "isRelevant = true"
---
name: step7_lng_iud_start_other
description: Calculated field to determine whether the woman who is being provided with an LNG-IUD can start the method now.
priority: 1
condition: "global_method_gender_type == 'Female' && step7_method_exit_f == 'lng_iud'"
actions:
  - "isRelevant = true"
---
name: step7_lng_iud_backup_needed
description: Calculated field to determine whether the woman who is being provided with an LNG-IUD right now, requires a back-up method.
priority: 1
condition: "step7_lng_iud_start_other == 0"
actions:
  - "isRelevant = true"
---
name: step7_toaster_start_method_no_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "step7_method_exit_f == 'lng_iud' && step7_lng_iud_start_other == 0 && step7_lng_iud_backup_needed == 0"
actions:
  - "isRelevant = true"
---
name: step7_toaster_a_backup_required
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "step7_method_exit_f == 'lng_iud' && step7_lng_iud_start_other == 0 && step7_lng_iud_backup_needed == 1"
actions:
  - "isRelevant = true"
---
name: step7_toaster_inserted_at_least
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "step7_method_exit_f == 'lng_iud' && step7_lng_iud_start_other == 1 && step3_current_method == 'ecp'"
actions:
  - "isRelevant = true"
---
name: step7_toaster_immediately_after_delivery
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "step7_method_exit_f == 'lng_iud' && step7_lng_iud_start_other == 1 && step3_postpartum == 1 && step3_days_postpartum <= 2"
actions:
  - "isRelevant = true"
---
name: step7_toaster_repeat_injection
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "step7_method_exit_f == 'lng_iud' && step7_lng_iud_start_other == 1 && (step3_current_method == 'dmpa_im' || step3_current_method == 'dmpa_sc' || step3_current_method == 'net_en')"
actions:
  - "isRelevant = true"
---
name: step7_implant_start_other
description: Calculated field to determine whether the woman who is being provided with an implant can start the method now.
priority: 1
condition: "step7_method_exit_f == 'etg_onerod' || step7_method_exit_f == 'lng_tworod'"
actions:
  - "isRelevant = true"
---
name: step7_implant_backup_needed
description: Calculated field to determine whether the woman who is being provided with an implant right now, requires a back-up method.
priority: 1
condition: "step7_implant_start_other == 0"
actions:
  - "isRelevant = true"
---
name: step7_toaster_one_rod_or
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'etg_onerod' || step7_method_exit_f == 'lng_tworod') && step7_implant_start_other == 0 && step7_implant_backup_needed == 0"
actions:
  - "isRelevant = true"
---
name: step7_toaster_one_rod_with_backup_needed
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'etg_onerod' || step7_method_exit_f == 'lng_tworod') && step7_implant_start_other == 0 && step7_implant_backup_needed == 1"
actions:
  - "isRelevant = true"
---
name: step7_toaster_one_rod_on_same_day
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'etg_onerod' || step7_method_exit_f == 'lng_tworod') && step7_implant_start_other == 1 && step3_current_method == 'ecp'"
actions:
  - "isRelevant = true"
---
name: step7_toaster_one_rod_repeat_injection
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'etg_onerod' || step7_method_exit_f == 'lng_tworod') && step7_implant_start_other == 1 && (step3_current_method == 'dmpa_im' || step3_current_method == 'dmpa_sc' || step3_current_method == 'net_en')"
actions:
  - "isRelevant = true"
---
name: step7_injectable_start_other
description: Calculated field to determine whether the woman who is being provided with an injectable can start the method now.
priority: 1
condition: "(step7_method_exit_f == 'dmpa_im' || step7_method_exit_f == 'dmpa_sc' || step7_method_exit_f == 'net_en')"
actions:
  - "isRelevant = true"
---
name: step7_injectable_backup_needed
description: Calculated field to determine whether the woman who is being provided with an injectable right now, requires a back-up method.
priority: 1
condition: "step7_injectable_start_other == 0"
actions:
  - "isRelevant = true"
---
name: step7_toaster_dmpa_no_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'dmpa_im' || step7_method_exit_f == 'dmpa_sc' || step7_method_exit_f == 'net_en') && step7_injectable_start_other == 0 && step7_injectable_backup_needed == 0"
actions:
  - "isRelevant = true"
---
name: step7_toaster_dmpa_need_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'dmpa_im' || step7_method_exit_f == 'dmpa_sc' || step7_method_exit_f == 'net_en') && step7_injectable_start_other == 0 && step7_injectable_backup_needed == 1"
actions:
  - "isRelevant = true"
---
name: step7_toaster_dmpa_provide_injection
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'dmpa_im' || step7_method_exit_f == 'dmpa_sc' || step7_method_exit_f == 'net_en') && step7_injectable_start_other == 1"
actions:
  - "isRelevant = true"
---
name: step7_pop_combined_method_backup_needed
description: Calculated field to determine whether the woman who is being provided with POP or a combined method right now, requires a back-up method.
priority: 1
condition: "(step7_method_exit_f == 'pop' || step7_method_exit_f == 'coc' || step7_method_exit_f == 'patch' || step7_method_exit_f == 'cvr')"
actions:
  - "isRelevant = true"
---
name: step7_toaster_pop_no_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'pop' && step7_pop_combined_method_backup_needed == 0)"
actions:
  - "isRelevant = true"
---
name: step7_toaster_pop_with_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'pop' && step7_pop_combined_method_backup_needed == 1)"
actions:
  - "isRelevant = true"
---
name: step7_toaster_coc_no_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'coc' || step7_method_exit_f == 'patch' || step7_method_exit_f == 'cvr') && step7_pop_combined_method_backup_needed == 0"
actions:
  - "isRelevant = true"
---
name: step7_toaster_coc_with_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'coc' || step7_method_exit_f == 'patch' || step7_method_exit_f == 'cvr') && step7_pop_combined_method_backup_needed == 1"
actions:
  - "isRelevant = true"
---
name: step7_toaster_pvr_no_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'pvr')"
actions:
  - "isRelevant = true"
---
name: step7_toaster_ecp_provide_pill
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'ecp')"
actions:
  - "isRelevant = true"
---
name: step7_toaster_lam_no_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'lam')"
actions:
  - "isRelevant = true"
---
name: step7_toaster_condom_no_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'male_condoms' || step7_method_exit_f == 'female_condoms' || step7_method_exit_m == 'withdrawal')"
actions:
  - "isRelevant = true"
---
name: step7_toaster_fab_with_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'fab')"
actions:
  - "isRelevant = true"
---
name: step7_toaster_male_ster_with_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_m == 'male_sterilization')"
actions:
  - "isRelevant = true"
---
name: step7_toaster_female_ster_with_backup
description: Note displayed to the health worker telling her when the client can start the method and whether a back-up is required, based on the when to start and backup method needed decision logic.
priority: 1
condition: "(step7_method_exit_f == 'female_sterilization')"
actions:
  - "isRelevant = true"
---
name: step7_iud_insertion_date
description: The date that the IUD was or will be inserted.
priority: 1
condition: "(step7_method_exit_f == 'cu_iud' || step7_method_exit_f == 'lng_iud')"
actions:
  - "isRelevant = true"
---
name: step7_implant_date
description: The date that the implants were or will be inserted.
priority: 1
condition: "(step7_method_exit_f == 'etg_onerod' || step7_method_exit_f == 'lng_tworod')"
actions:
  - "isRelevant = true"
---
name: step7_last_injection_date
description: The date that the last injection was done.
priority: 1
condition: "(step7_method_exit_f == 'dmpa_im' || step7_method_exit_f == 'dmpa_sc' || step7_method_exit_f == 'net_en')"
actions:
  - "isRelevant = true"
---
name: step7_sterilization_date
description: The date that the sterilization procedure was done.
priority: 1
condition: "(step7_method_exit_f == 'female_sterilization' || step7_method_exit_m == 'male_sterilization')"
actions:
  - "isRelevant = true"
---
name: step7_reason_different_chosen_exit_method
description: The date that the sterilization procedure was done.
priority: 1
condition: "(step7_method_exit_f != step6_method_chosen_f || step7_method_exit_m == step6_method_chosen_m)"
actions:
  - "isRelevant = true"
---
name: step7_reason_different_chosen_exit_method_other
description: The date that the sterilization procedure was done.
priority: 1
condition: "step7_reason_different_chosen_exit_method.contains('other')"
actions:
  - "isRelevant = true"
---
name: step7_reason_no_method_exit
description: The date that the sterilization procedure was done.
priority: 1
condition: "(step7_method_exit_f == 'no_method' || step7_method_exit_m == 'no_method')"
actions:
  - "isRelevant = true"
---
name: step7_reason_no_method_exit_other
description: The date that the sterilization procedure was done.
priority: 1
condition: "step7_reason_no_method_exit.contains('other')"
actions:
  - "isRelevant = true"